<template>
  
</template>

<script>
  var index = new Vue({
  el: '#index',
  data: {
  articles: []
  },
  mounted: function() {
  const self = this;
  fetch("/articles.json")
  .then(articles => articles.json())
  .then(articles => console.log(articles))
  .then(articles => self.files = articles);
  }
  }) 
</script>